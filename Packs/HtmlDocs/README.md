---
name: HtmlDocs
pack-id: pai-htmldocs-v1.0.0
version: 1.0.0
author: larsboes
description: "Use when creating standalone HTML documentation sites for projects — architecture overviews, feature catalogs, component references, or configuration docs. Zero-build, no framework, just HTML/CSS/JS."
type: skill
platform: pi,claude-code
source: PAI Fork (larsboes)
---

# HtmlDocs

Use when creating standalone HTML documentation sites for projects — architecture overviews, feature catalogs, component references, or configuration docs. Zero-build, no framework, just HTML/CSS/JS.

## Installation

Point your AI at this directory:

```
"Install the HtmlDocs pack from PAI/Packs/HtmlDocs/"
```

Your AI reads `INSTALL.md` and walks through the wizard: system check, dependency verification, installation, verification.

## What's Included

```
  SKILL.md
  references/
    content-patterns.md
    templates/
      shared.css
      shared.js
  scripts/
    scaffold.sh
    validate.sh
```

The skill source is `SKILL.md`. See `INSTALL.md` for setup and `VERIFY.md` to confirm everything works.

## Source

Original skill created for PAI Fork — not in upstream danielmiessler/Personal_AI_Infrastructure.

## License

MIT — see [PAI LICENSE](../../LICENSE).
